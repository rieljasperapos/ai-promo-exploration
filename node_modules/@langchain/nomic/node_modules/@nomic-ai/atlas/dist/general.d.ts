import type { AtlasUser } from './user.js';
export declare const isNode: string | false;
export declare abstract class BaseAtlasClass {
    user: AtlasUser;
    constructor(user?: AtlasUser);
    apiCall(endpoint: string, method: 'GET' | 'POST', payload?: Atlas.Payload, headers?: null | Record<string, string>): Promise<string | any[] | Record<string, any> | Uint8Array | import("apache-arrow").Table<any>>;
}
